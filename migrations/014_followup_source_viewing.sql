-- 014: 跟进记录记下"这条提醒是哪次带看产生的"（2026-09-25，配合 record_viewing 的回访提醒幂等）
-- 背景：带看标记完成会自动写一条 type=reminder 的回访提醒，但重复记"完成"（先报结果、回头补一句反馈）
-- 会一次一条地堆出多条一模一样的提醒，经纪人看到同一件事报两遍、逾期清单里也重复。
-- 有了这一列，重复记完成时能精确找到"这次带看的那条提醒"，只更新它、不重复建。
-- 幂等：加列语句由 migrate.py 自动跳过已存在的列；全新库由 Base.metadata.create_all()
-- 直接建带这一列的表，这条迁移只给已有库补齐。
-- （注意：本文件的注释里不要出现完整的 SQL 语句字样 —— migrate.py 的幂等判定是按正则扫整段
--   文本，注释里的假语句会被当成真语句去判重，反而让真语句被跳过判定失败。）
ALTER TABLE re_followups ADD COLUMN source_viewing_id INTEGER;
